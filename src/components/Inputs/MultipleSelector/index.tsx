import MultipleSelectorError from "./MultipleSelectorError";
import MultipleSelectorInput from "./MultipleSelectorInput";
import MultipleSelectorRoot from "./MultipleSelectorRoot";

export const MultipleSelector = {
    Root: MultipleSelectorRoot,
    Input: MultipleSelectorInput,
    Error: MultipleSelectorError
}