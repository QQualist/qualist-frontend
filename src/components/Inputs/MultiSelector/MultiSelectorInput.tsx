import MultipleSelector, {
  MultipleSelectorProps,
} from "@/components/ui/muti-selector";

const MultiSelectorInput = ({ ...props }: MultipleSelectorProps) => {
  return <MultipleSelector {...props} />;
};

export default MultiSelectorInput;
